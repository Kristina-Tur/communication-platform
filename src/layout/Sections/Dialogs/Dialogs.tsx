// @flow
import * as React from 'react';
import {MessageForm} from "./MessageForm";

type Props = {};
export const Dialogs = (props: Props) => {
    return (
        <div>
            <MessageForm/>
            {/*<DialogList/>*/}
        </div>
    );
};